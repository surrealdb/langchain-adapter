services:
  surrealdb:
    image: surrealdb/surrealdb:v3.2.4
    command: start --log trace --user root --pass root memory
    ports:
      - "8000:8000"
    environment:
      SURREAL_USER: root
      SURREAL_PASS: root
